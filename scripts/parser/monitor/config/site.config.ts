import {SiteOptions} from "../types/types";

const settings: SiteOptions = {
    resolution: [[2580, 1090], null],
    refreshRate: [120, null],
    displaySize: [25, null],
    pixelType: ['IPS'],
    price: [0, 58000]
};

export default settings;
